# Frontend

The frontend was built with Electron, React with TypeScript, and Vite Using Electron Forge.

## Setup

Prerequisites:
- Node.js
- npm

To install dependencies, use:
```bash
cd frontend
npm install
```

To start the app, use:
```bash
npm start
```
